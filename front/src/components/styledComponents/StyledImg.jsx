import { styled } from "@mui/material/styles";

const StyledImg = styled("img")({
  maxWidth: "100%",
  height: "auto",
  marginTop: "8px",
  cursor: "pointer",
  borderRadius: "6px",
});

export default StyledImg;
